calc_area <- function(x2 = x2){
  
  # approximate the size of a cell value. output = km2
  
  # sum area of forest
  sum(values(area(x2))[which(values(x2))])
  
  # sum area of non-forest
  sum(values(area(x2))[which(values(x2))])
  
  
  sum(values(area(x2))[which(values(x2))])
  
  sum(values(area(x2))[which(values(x2))])
  
  
  return(table_forest)
}